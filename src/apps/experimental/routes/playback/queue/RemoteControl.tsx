import React, { FunctionComponent } from 'react';

// import globalize from 'scripts/globalize';

const RemoteControl: FunctionComponent = () => {
    return (
        <div className='remoteControlSection'></div>
    );
};

export default RemoteControl;
